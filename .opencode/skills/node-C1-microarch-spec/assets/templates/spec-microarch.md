# 微架构规格模板（Node C1）

> 用途：每模块一份，指导 RTL 编码。存入 `docs/microarch/<module>.md` 或 `ip/<ip>/AIFlow/doc/microarch.md`。

## 1. 模块概述
- 功能 / 在系统中的位置 / 挂载总线

## 2. 接口与端口
- 端口列表（信号、宽度、方向）
- 时钟/复位/使能

## 3. 寄存器定义（Regmap）
| 偏移 | 名称 | R/W | 位宽 | 复位值 | 描述 |
|------|------|-----|------|--------|------|

## 4. 状态机
- 状态转移图（文本或 mermaid）
- 状态描述与转移条件

## 5. 数据通路与流水线
- 流水线级数、各段功能
- 握手协议（valid/ready 时序）

## 6. 跨时钟域
- 时钟域划分、同步策略（对应 C5 CDC）

## 7. 关键时序
- 关键路径描述
- 性能约束（时延/吞吐）

## 8. 接口契约链接
- 指向 C2 模块接口契约文档
# Open Issue 清单

> 节点 A1 产物（后续节点新增条目追加登记）| 归属：spec-agent | 版本：v0.4
> **DoD 要求**：场景清单无未决 open issue —— 本清单所有条目须由人类（产品/市场/技术负责人）答复关闭后，A1 方可评审通过。
> **当前状态**：OI-A1-001~005 已于 2026-08-19 由用户（人类）人工答复并全部关闭（见 §2 已关闭问题表），决策摘要已折入 `PRD.md` 与 `use-cases.md`，A1-D1 已满足；A3 新增 OI-A3-006 已于 2026-08-20 由用户裁定关闭（见 §2，PPAC-013 放宽至 ≤100ms）。当前 0 项 open。
> **OI 编号规范（ADR-008）**：OI 为"全流程需求/规格歧义澄清"编号，不限定 A1；格式 `OI-<节点ID>-<全局序号>`（序号项目全局递增，不随节点重置）。本文件原条目出自 A1（OI-A1-001~005）；后续节点产生的新歧义接续全局序号（A2 后为 006…），如 A3 的 OI-A3-006。

## 1. 开放问题清单

### 1.1 A3 新增（2026-08-20，条目已关闭见 §2）

| OI | 问题描述 | 影响范围 | 待答复角色 | 状态 |
|----|---------|---------|-----------|------|
| OI-A3-006 | **SPI Flash 启动加载带宽 vs PPAC-013 启动时延矛盾**（已关闭，决策摘要见 §2） | PPAC-006/PPAC-013 指标冲突；BOOT 通路带宽（BLOCK-05/13） | 用户 | closed（2026-08-20） |

### 1.2 原 A1 条目

| OI | 问题描述 | 影响范围 | 待答复角色 | 状态 |
|----|---------|---------|-----------|------|
| OI-A1-001 | **程序/数据存储方案**：MCU 需要非易失存储承载固件 + 易失存储承载运行数据。需求输入未明确：是否含片上 Flash？SRAM 容量预期？启动从何处取指？固件更新写入的目标介质是什么？ | REQ-011、REQ-003（固件更新机制）、REQ-004（调试范围）、启动流程、B1/B2 架构 | 人类（产品/硬件） | closed（2026-08-19） |
| OI-A1-002 | **tinyRISCV 核接入版本与方式**：需 pin 定 tinyRISCV 的具体版本/tag（或 fork 提交）、RIB 总线在 SoC 中的沿用方式（6 外设 × 256MB 映射是否保留）。无标准 Debug Module 对 JTAG 调试方案（REQ-004）的影响需评估。 | REQ-001、REQ-004、B6 集成规划、C0 合同验证 | 人类（技术负责人）+ 待 C0 验证 | closed（2026-08-19） |
| OI-A1-003 | **GPIO 模块是否纳入范围**：必选清单未列 GPIO，但 SPI/IIC/PWM 引脚往往需引脚复用/GPIO 能力。是否需要独立 GPIO 外设？ | A3 接口规格、REQ-012、外设互操作 | 人类（产品） | closed（2026-08-19） |
| OI-A1-004 | **目标工艺/库**：可综合性与 A2 时序指标（Fmax）依赖目标工艺与综合工具链（开源综合 vs 商用 DC/GENUS）。本实验是否已有指定工艺库？ | REQ-013、REQ-016、A2 指标、E/F 阶段 | 人类（实验环境） | closed（2026-08-19） |
| OI-A1-005 | **验收演示目标**：最终收敛的演示形态（如 CoreMark 跑分演示、LED 点灯 + 串口打印、bootloader 更新 demo）？用于定义验证终点与优先级平衡。 | REQ-013、D 阶段验证范围、演示基准 | 人类（产品/实验） | closed（2026-08-19） |

## 2. 已关闭问题（记录）

| OI | 问题 | 决策摘要（人类答复，2026-08-19 已确认） | 关闭方式 | 关闭人 | 日期 |
|----|------|----------------------------------------|---------|--------|------|
| OI-A3-006 | SPI Flash 启动带宽 vs PPAC-013 启动时延 | **PPAC-013 放宽至 ≤ 100 ms**（单线 SPI @ Fsys/4=12.5 MHz，70% 评估 ≈ 60 ms 留裕量）；保持单线 SPI；高速 SPI（160 MHz 级 QSPI）登记为 B1 时钟架构评估项 | 人工答复 | 用户 | 2026-08-20 |
| OI-A1-001 | 程序/数据存储方案 | **SPI Flash（固件非易失存储）+ AXI_SRAM（运行数据）**。启动取指自 Flash（经 SPI 加载），固件更新写入 Flash。注意：OI-A1-002 选定 RIB↔AXI 桥，SRAM 落在 AXI 侧（AXI_SRAM）。 | 人工答复 | 用户 | 2026-08-19 |
| OI-A1-002 | tinyRISCV 核接入版本与方式 | **pin 定版本**（具体 commit/tag 留待 C0 验证时定，文档标注"待 C0 定"）；**保留 RIB 总线**，SoC 层**自研 RIB↔AXI 桥**接入标准 AXI 外设；无标准 Debug Module 对 JTAG 的影响标注"待 C0 评估"。 | 人工答复 | 用户 | 2026-08-19 |
| OI-A1-003 | GPIO 模块是否纳入范围 | **纳入独立 GPIO 外设**（引脚复用，SPI/IIC/PWM 与 GPIO 协同）。 | 人工答复 | 用户 | 2026-08-19 |
| OI-A1-004 | 目标工艺/库 | **工艺/库暂不定**，功能收敛以仿真为主，E/F 阶段前再定；REQ-013/016 相关指标保持"仿真可验证"口径。 | 人工答复 | 用户 | 2026-08-19 |
| OI-A1-005 | 验收演示目标 | **验收演示 = LED 点灯 + 串口打印日志 + SPI/IIC/PWM 外设联动**。 | 人工答复 | 用户 | 2026-08-19 |

## 3. 说明

- **AI 不代猜需求**：以上问题均为需求级歧义，由人类答复；AI 提供影响分析与选项建议，决策归属人类。
- 关闭后状态更新为 `closed`，并同步更新 `spec-001-PRD.md` / `spec-002-use-cases.md` 相关条目（REQ-011/REQ-018/REQ-019/REQ-020 等）。
- 所有涉及 tinyRISCV 核的契约细节（版本 pin、RIB↔AXI 桥实现、Debug Module 对 JTAG 的影响）在文档中标注"待 C0 验证"并移交 B6/C0 处理，不影响 A1 需求层面收敛。
- A1 判据"场景无未决 open issue"：当前 0 项 open，已满足。

## 4. 变更记录

| 版本 | 日期 | 变更 | 作者 |
|------|------|------|------|
| v0.1 | 2026-08-19 | 初稿：5 项 open issue | spec-agent |
| v0.2 | 2026-08-19 | OI-A1-001~005 全部关闭（人工答复，用户，2026-08-19），登记已关闭问题表并折入 PRD/UC | spec-agent |
| v0.3 | 2026-08-20 | 标题与说明改为全流程 OI 登记（ADR-008）；新增 A3 条目 OI-A3-006（SPI Flash 启动带宽 vs PPAC-013 矛盾，open 待裁定） | spec-agent |
| v0.4 | 2026-08-20 | OI-A3-006 用户裁定关闭（PPAC-013 ≤100ms），§1.1 状态同步、§2 已关闭表登记 | spec-agent |